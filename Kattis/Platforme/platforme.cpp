#include <iostream>
#include <vector>

using namespace std;

typedef pair<int, int> ii;

int main() {

	ios::sync_with_stdio(false);
	cin.tie(nullptr);

	int N;
	cin >> N;
	vector<int> max_height(10000, 0);
	vector<ii> V;
	int ans = 0;

	for (int case_num = 0; case_num < N; ++case_num) {
		int Y, X1, X2;
		cin >> Y >> X1 >> X2;


	}

	return 0;
}