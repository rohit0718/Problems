#include <iostream>
#include <vector>

using namespace std;


int main() {

	ios::sync_with_stdio(false);
	cin.tie(nullptr);

	int start[4][4];
	int end[4][4];

	for (int i = 0; i < 4; i++) {
		for (int j = 0; j < 4; j++) {
			cin >> start[i][j];
		}  		 	  	  
	}

	int curr[4];
	for in




	return 0;
}